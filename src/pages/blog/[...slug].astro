---
import { getCollection } from 'astro:content'
import GeneralLayout from '@/layouts/General/index.astro'

export async function getStaticPaths() {
	const blogEntries = await getCollection('blog')
	return blogEntries.map((entry) => ({
		params: { slug: entry.slug },
		props: { entry },
	}))
}

const { entry } = Astro.props
const { Content } = await entry.render()
---

<GeneralLayout title={`✏️ ${entry.data.title}`}>
	<article class="space-y-2">
		<h1 class="mt-0">📰 {entry.data.title}</h1>
		<div class="flex flex-col gap-0 lg:flex-row lg:gap-4">
			<p class="mb-0">
				<span>on <b>{entry.data.date.toLocaleDateString()}</b></span>
				<span>by <b>crowdozer</b></span>
			</p>
			<div class="flex flex-row flex-wrap gap-2">
				{
					entry.data.tags.map((tag) => (
						<a class="button-micro" href={`/blog/tags/${tag}`}>
							🔖{tag}
						</a>
					))
				}
			</div>
		</div>
		<hr />
		<div class="mx-auto max-w-4xl py-8">
			<Content />
		</div>
	</article>
	<a href="/blog">{'< back to index'}</a>
</GeneralLayout>

<style is:global>
	/* Sakura.css v1.5.0
	* ================
	* Minimal css theme.
	* Project: https://github.com/oxalorg/sakura/
	* 
	* ---
	*
	* note: i modified this
	* source: https://unpkg.com/sakura.css/css/sakura-dark.css
	*/

	/* Body */
	article {
		font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
			'Helvetica Neue', Arial, 'Noto Sans', sans-serif;
	}

	article {
		color: #c9c9c9;
	}

	article h1,
	article h2,
	article h3,
	article h4,
	article h5,
	article h6 {
		line-height: 1.1;
		font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
			'Helvetica Neue', Arial, 'Noto Sans', sans-serif;
		font-weight: 700;
		margin-top: 3rem;
		margin-bottom: 1.5rem;
		overflow-wrap: break-word;
		word-wrap: break-word;
		-ms-word-break: break-all;
		word-break: break-word;
	}

	article h1 {
		font-size: 2.35em;
	}

	article h2 {
		font-size: 2em;
	}

	article h3 {
		font-size: 1.75em;
	}

	article h4 {
		font-size: 1.5em;
	}

	article h5 {
		font-size: 1.25em;
	}

	article h6 {
		font-size: 1em;
	}

	article p {
		margin-top: 0px;
		margin-bottom: 2.5rem;
	}

	article small,
	article sub,
	article sup {
		font-size: 75%;
	}

	article hr {
		border-color: #ffffff;
	}

	/* article a {
		text-decoration: none;
		color: #ffffff;
	}
	article a:visited {
		color: #e6e6e6;
	}
	article a:hover {
		color: #c9c9c9;
		border-bottom: 2px solid #c9c9c9;
	} */

	article ul {
		padding-left: 1.4em;
		margin-top: 0px;
		margin-bottom: 2.5rem;
	}

	article li {
		margin-bottom: 0.4em;
	}

	article blockquote {
		margin-left: 0px;
		margin-right: 0px;
		padding-left: 1em;
		padding-top: 0.8em;
		padding-bottom: 0.8em;
		padding-right: 0.8em;
		border-left: 5px solid theme(colors.green.500);
		background-color: theme(colors.neutral-dark-highlight);
	}

	article blockquote p {
		margin-bottom: 0;
	}

	article img,
	article video {
		height: auto;
		max-width: 100%;
		margin-top: 0px;
		margin-bottom: 2.5rem;
	}

	/* Pre and Code */
	article pre {
		background-color: #4a4a4a;
		display: block;
		padding: 1em;
		overflow-x: auto;
		margin-top: 0px;
		margin-bottom: 2.5rem;
		font-size: 0.9em;
	}

	article code,
	article kbd,
	article samp {
		font-size: 0.9em;
		padding: 0 0.5em;
		background-color: #4a4a4a;
		white-space: pre-wrap;
	}

	article pre > code {
		padding: 0;
		background-color: transparent;
		white-space: pre;
		font-size: 1em;
	}

	/* Tables */
	article table {
		text-align: justify;
		width: 100%;
		border-collapse: collapse;
		margin-bottom: 2rem;
	}

	article td,
	article th {
		padding: 0.5em;
		border-bottom: 1px solid #4a4a4a;
	}

	/* Buttons, forms and input */
	article input,
	article textarea {
		border: 1px solid #c9c9c9;
	}
	article input:focus,
	article textarea:focus {
		border: 1px solid #ffffff;
	}

	article textarea {
		width: 100%;
	}

	article .button,
	article button,
	article input[type='submit'],
	article input[type='reset'],
	article input[type='button'],
	article input[type='file']::file-selector-button {
		display: inline-block;
		padding: 5px 10px;
		text-align: center;
		text-decoration: none;
		white-space: nowrap;
		background-color: #ffffff;
		color: #222222;
		border-radius: 1px;
		border: 1px solid #ffffff;
		cursor: pointer;
		box-sizing: border-box;
	}
	article .button[disabled],
	article button[disabled],
	article input[type='submit'][disabled],
	article input[type='reset'][disabled],
	article input[type='button'][disabled],
	article input[type='file']::file-selector-button[disabled] {
		cursor: default;
		opacity: 0.5;
	}
	article .button:hover,
	article button:hover,
	article input[type='submit']:hover,
	article input[type='reset']:hover,
	article input[type='button']:hover,
	article input[type='file']::file-selector-button:hover {
		background-color: #c9c9c9;
		color: #222222;
		outline: 0;
	}
	article .button:focus-visible,
	article button:focus-visible,
	article input[type='submit']:focus-visible,
	article input[type='reset']:focus-visible,
	article input[type='button']:focus-visible,
	article input[type='file']::file-selector-button:focus-visible {
		outline-style: solid;
		outline-width: 2px;
	}

	article textarea,
	article select,
	article input {
		color: #c9c9c9;
		padding: 6px 10px; /* The 6px vertically centers text on FF, ignored by Webkit */
		margin-bottom: 10px;
		background-color: #4a4a4a;
		border: 1px solid #4a4a4a;
		border-radius: 4px;
		box-shadow: none;
		box-sizing: border-box;
	}
	article textarea:focus,
	article select:focus,
	article input:focus {
		border: 1px solid #ffffff;
		outline: 0;
	}

	article input[type='checkbox']:focus {
		outline: 1px dotted #ffffff;
	}

	article label,
	article legend,
	article fieldset {
		display: block;
		margin-bottom: 0.5rem;
		font-weight: 600;
	}
</style>
